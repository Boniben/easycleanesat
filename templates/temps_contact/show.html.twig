{% extends 'base.html.twig' %}

{% block title %}TempsContact{% endblock %}

{% block body %}
    <h1>TempsContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temps_contact.id }}</td>
            </tr>
            <tr>
                <th>TempsContact</th>
                <td>{{ temps_contact.tempsContact }}</td>
            </tr>
            <tr>
                <th>Picto</th>
                <td>{{ temps_contact.picto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_temps_contact_index') }}">back to list</a>

    <a href="{{ path('app_temps_contact_edit', {'id': temps_contact.id}) }}">edit</a>

    {{ include('temps_contact/_delete_form.html.twig') }}
{% endblock %}
