{% extends 'base.html.twig' %}

{% block title %}MeoProduit{% endblock %}

{% block body %}
    <h1>MeoProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meo_produit.id }}</td>
            </tr>
            <tr>
                <th>VolumeProduit</th>
                <td>{{ meo_produit.volumeProduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_meo_produit_index') }}">back to list</a>

    <a href="{{ path('app_meo_produit_edit', {'id': meo_produit.id}) }}">edit</a>

    {{ include('meo_produit/_delete_form.html.twig') }}
{% endblock %}
