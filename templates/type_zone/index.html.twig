{% extends 'base.html.twig' %}

{% block title %}Types de zone
{% endblock %}

{% block body %}
	<div class="page-header">
		<h1>Types de zone</h1>
		{% if app.user and is_granted('ROLE_ADMIN') %}
			<a href="{{ path('app_type_zone_new') }}" class="btn btn-primary">+ Ajouter un type</a>
		{% endif %}
	</div>

	<div class="table-wrapper">
		<table>
			<thead>
				<tr>
					<th>#</th>
					<th>Nom</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				{% for type_zone in type_zones %}
					<tr>
						<td>{{ loop.index }}</td>
						<td>{{ type_zone.nom }}</td>
						<td>
							<div class="btn-group">
								<a href="{{ path('app_type_zone_show', {'id': type_zone.id}) }}" class="btn btn-outline btn-sm">Voir</a>
								{% if app.user and is_granted('ROLE_ADMIN') %}
									<a href="{{ path('app_type_zone_edit', {'id': type_zone.id}) }}" class="btn btn-secondary btn-sm">Modifier</a>
								{% endif %}
							</div>
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="3">Aucun type de zone trouvé</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
