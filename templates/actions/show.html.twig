{% extends 'base.html.twig' %}

{% block title %}Actions{% endblock %}

{% block body %}
    <h1>Actions</h1>

    <table class="table">
        <thead>
            <th>Code</th>
            <th>Nom</th>
        </thead>

        <tbody>
        <td>
        {% for necessaire in action.necessaire %}
            <hr>{{necessaire.code}}<br>
            {% endfor %}
        </td>
        <td>
            {% for necessaire in action.necessaire %}
            <hr>{{necessaire.nom}}<br>
            
            {% endfor %}
        </td>

        </tbody>
    </table>

    <a href="{{ path('app_actions_index') }}">back to list</a>

    <a href="{{ path('app_actions_edit', {'id': action.id}) }}">edit</a>

    {{ include('actions/_delete_form.html.twig') }}
{% endblock %}
