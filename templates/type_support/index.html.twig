{% extends 'base.html.twig' %}

{% block title %}TypeSupport index{% endblock %}

{% block body %}
    <h1>TypeSupport index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Picto</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_support in type_supports %}
            <tr>
                <td>{{ type_support.id }}</td>
                <td>{{ type_support.nom }}</td>
                <td>{{ type_support.picto }}</td>
                <td>
                    <a href="{{ path('app_type_support_show', {'id': type_support.id}) }}">show</a>
                    <a href="{{ path('app_type_support_edit', {'id': type_support.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_support_new') }}">Create new</a>
{% endblock %}
