{% extends 'base.html.twig' %}

{% block title %}Type de support{% endblock %}

{% block body %}
    <h1>Type de support</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Picto</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_support in type_supports %}
            <tr>
                <td>{{ type_support.id }}</td>
                <td>{{ type_support.nom }}</td>
                <td>{{ type_support.picto }}</td>
                <td>
                    <a href="{{ path('app_type_support_show', {'id': type_support.id}) }}">Voir</a>
                    <a href="{{ path('app_type_support_edit', {'id': type_support.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_support_new') }}">Créer un nouveau type de support</a>
{% endblock %}
