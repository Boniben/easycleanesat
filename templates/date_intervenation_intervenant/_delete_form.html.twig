<form method="post" action="{{ path('app_date_intervenation_intervenant_delete', {'id': date_intervenation_intervenant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date_intervenation_intervenant.id) }}">
    <button class="btn">Delete</button>
</form>
