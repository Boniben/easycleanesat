{% extends 'base.html.twig' %}

{% block title %}DateIntervenationIntervenant{% endblock %}

{% block body %}
    <h1>DateIntervenationIntervenant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ date_intervenation_intervenant.id }}</td>
            </tr>
            <tr>
                <th>Date_intervention</th>
                <td>{{ date_intervenation_intervenant.dateIntervention ? date_intervenation_intervenant.dateIntervention|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_date_intervenation_intervenant_index') }}">back to list</a>

    <a href="{{ path('app_date_intervenation_intervenant_edit', {'id': date_intervenation_intervenant.id}) }}">edit</a>

    {{ include('date_intervenation_intervenant/_delete_form.html.twig') }}
{% endblock %}
