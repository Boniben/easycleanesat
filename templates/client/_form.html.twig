{{ form_start(form) }}
<div class="form-group">
	{{ form_row(form.nom, {'label': 'Nom du client', 'attr': {'placeholder': 'Ex: Lactalis, Intermarché...'}}) }}
</div>
<div class="form-actions">
	<button class="btn btn-primary" type="submit">{{ button_label|default('Sauvegarder') }}</button>
	<a href="{{ path('app_client_index') }}" class="btn btn-secondary">Annuler</a>
</div>
{{ form_end(form) }}
