{% extends 'base.html.twig' %}

{% block title %}Consommable{% endblock %}

{% block body %}
    <h1>Consommable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consommable.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ consommable.nom }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ consommable.code }}</td>
            </tr>
            <tr>
                <th>Picto</th>
                <td>{{ consommable.picto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consommable_index') }}">back to list</a>

    <a href="{{ path('app_consommable_edit', {'id': consommable.id}) }}">edit</a>

    {{ include('consommable/_delete_form.html.twig') }}
{% endblock %}
