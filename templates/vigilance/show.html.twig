{% extends 'base.html.twig' %}

{% block title %}Vigilance{% endblock %}

{% block body %}
    <h1>Vigilance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vigilance.id }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ vigilance.definition }}</td>
            </tr>
            <tr>
                <th>Picto</th>
                <td>{{ vigilance.picto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vigilance_index') }}">back to list</a>

    <a href="{{ path('app_vigilance_edit', {'id': vigilance.id}) }}">edit</a>

    {{ include('vigilance/_delete_form.html.twig') }}
{% endblock %}
