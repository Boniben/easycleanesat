{% extends 'base.html.twig' %}

{% block title %}Redacteur{% endblock %}

{% block body %}
    <h1>Redacteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ redacteur.id }}</td>
            </tr>
            <tr>
                <th>Initial</th>
                <td>{{ redacteur.initial }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_redacteur_index') }}">back to list</a>

    <a href="{{ path('app_redacteur_edit', {'id': redacteur.id}) }}">edit</a>

    {{ include('redacteur/_delete_form.html.twig') }}
{% endblock %}
