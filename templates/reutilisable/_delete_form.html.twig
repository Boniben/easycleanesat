<form method="post" action="{{ path('app_reutilisable_delete', {'id': reutilisable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reutilisable.id) }}">
    <button class="btn">Delete</button>
</form>
