{% extends 'base.html.twig' %}

{% block title %}Plage{% endblock %}

{% block body %}
    <h1>Plage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plage.id }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ plage.heureDebut ? plage.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeurFin</th>
                <td>{{ plage.heurFin ? plage.heurFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plage_index') }}">back to list</a>

    <a href="{{ path('app_plage_edit', {'id': plage.id}) }}">edit</a>

    {{ include('plage/_delete_form.html.twig') }}
{% endblock %}
