<form method="post" action="{{ path('app_handicap_delete', {'id': handicap.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ handicap.id) }}">
    <button class="btn">Delete</button>
</form>
