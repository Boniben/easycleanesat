{% extends 'base.html.twig' %}

{% block title %}SupportClient{% endblock %}

{% block body %}
    <h1>SupportClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support_client.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_support_client_index') }}">back to list</a>

    <a href="{{ path('app_support_client_edit', {'id': support_client.id}) }}">edit</a>

    {{ include('support_client/_delete_form.html.twig') }}
{% endblock %}
