{% extends 'base.html.twig' %}

{% block title %}ZonesClient{% endblock %}

{% block body %}
    <h1>ZonesClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zones_client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ zones_client.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zones_client_index') }}">back to list</a>

    <a href="{{ path('app_zones_client_edit', {'id': zones_client.id}) }}">edit</a>

    {{ include('zones_client/_delete_form.html.twig') }}
{% endblock %}
